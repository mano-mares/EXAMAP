class Question {
  String id;
  String questionText;
  int maxPoint;
  String questionType;

  Question(
      {required this.id,
      required this.questionText,
      required this.maxPoint,
      required this.questionType});
}
